apiVersion: v1
kind: Service
metadata:
  name: postgres-db
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: {{ .Values.database.databasePort }}
      targetPort: {{ .Values.database.databaseTargetPort }}